[phases.setup]
# Use correct Node version (required by Vite 7, React Router 7)
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmd = "npm ci"

[phases.build]
cmd = "npm run build"

[start]
# Use our custom Caddyfile
cmd = "caddy run --config public/Caddyfile --adapter caddyfile"
